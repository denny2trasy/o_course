<h2>课程列表</h2><span><%= link_to("返回>>", request.referer) unless request.referer.blank? %></span>
<ul>
	<% @courses.each do |course| %>
	<li><%= link_to course.name, item_group_course_path(@item_group, course) %></li>
	<% end %>
</ul>